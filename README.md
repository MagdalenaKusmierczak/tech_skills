To call data from database 'employee' with certain key e.g. "firstName", call getEmployeeInfo(employee, "firstName")
To call nested data e.g. firstName of manager call getEmployeeInfo(employee, "manager.firstName")
